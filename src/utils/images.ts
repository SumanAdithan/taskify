// Importing image assets
import logo from '@assets/images/.png';

// Defining the shape of the image assets object
interface ImageAssets {
    logo: string;
}

// Exporting the image assets
export const images: ImageAssets = {
    logo,
};
