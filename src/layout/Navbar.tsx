import { FC } from 'react';

const Navbar: FC = () => {
    return (
        <nav className='bg-background p-8 text-black shadow-md'>
            <h1 className='text-xl'>Taskify</h1>
        </nav>
    );
};

export default Navbar;
